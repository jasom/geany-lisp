include_rules

include $(TOP)/includeme.tup

CFLAGS=@(CFLAGS) -Wall -Wextra -Werror -shared -fPIC `pkg-config --cflags geany`

: foreach *.c | local.h |> gcc $(CFLAGS) -c -o %o %f |> %B.o
: *.o |> gcc $(CFLAGS) -o %o %f |> glisp.so
